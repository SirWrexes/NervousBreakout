local lfs = require "lfs"

local dirsep = package.config:sub(1, 1)
local cwd = lfs.currentdir()
local path = table.concat({
    table.concat({ cwd, "?.lua" }, dirsep),
    table.concat({ cwd, "?", "init.lua" }, dirsep),
    package.path,
}, ";")

package.path = path

require "lib.Util"

return {
    _all = {
        coverage = true,
    },
    default = {
        ROOT = {
            "./test",
        },
        pattern = "test.lua",
        verbose = true,
        coverage = true,
        output = "plainTerminal",
    },
}
